// Separate class required because you cannot mix
// 'with sharing' and 'without sharing' in the same class
public without sharing class GuestFileInserter {
    public static void insertCV(ContentVersion cv) {
        insert cv;
    }
}